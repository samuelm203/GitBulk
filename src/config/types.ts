/**
 * Öffentliche Type-Definitionen der GitBulk-Konfiguration.
 *
 * Diese Datei wird von TS-Konsumenten importiert und in JS-Configs
 * via JSDoc-Hints verwendet:
 *
 * @example JS-Config mit Type-Checking via JSDoc
 * ```js
 * /** @type {import('gitbulk/config').GitBulkConfig} *\/
 * export default {
 *   repositories: [...],
 * };
 * ```
 */

// TODO: Hier werden die aus Zod abgeleiteten Typen re-exportiert.
// export type { GitBulkConfig, RepositoryConfig, GitOperation } from './schema.js';

export {};
